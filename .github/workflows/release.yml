name: Create Release Package

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      # --- Start of Custom Manual Packaging ---
      - name: Create staging directory for archive contents
        run: mkdir staging

      - name: Copy project files to staging area
        run: |
          cp -r uc_intg_xbox staging/
          cp pyproject.toml driver.json README.md staging/

      - name: Create the required bin/uc-intg-xbox script
        run: |
          mkdir -p staging/bin
          echo '#!/bin/sh' > staging/bin/uc-intg-xbox
          echo 'python3 -m uc_intg_xbox.driver' >> staging/bin/uc-intg-xbox
          chmod +x staging/bin/uc-intg-xbox

      - name: Create the final .tar.gz archive
        run: tar -czf uc-intg-xbox-${{ steps.get_version.outputs.VERSION }}.tar.gz -C staging .
      # --- End of Custom Packaging ---

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./uc-intg-xbox-${{ steps.get_version.outputs.VERSION }}.tar.gz
          asset_name: uc-intg-xbox-${{ steps.get_version.outputs.VERSION }}.tar.gz
          asset_content_type: application/gzip