{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Xbox Driver",
            "type": "python",
            "request": "launch",
            "module": "uc_intg_xbox.driver",
            "justMyCode": true,
            "env": { "PYTHONPATH": "${workspaceFolder}" }
        }
    ]
}