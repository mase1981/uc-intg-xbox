[project]
name = "uc_intg_xbox"
version = "1.0.17"
authors = [
  { name="Meir Miyara", email="meir.miyara@gmail.com" },
]
description = "An Unfolded Circle integration for Xbox"
readme = "README.md"
requires-python = ">=3.10"
# Dependencies are now managed by requirements.txt for the build
dependencies = []